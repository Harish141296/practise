"""
git checkout -b new-branch
# Make changes, then:
git add .
git commit -m "Your commit message"
git checkout main
git merge new-branch
# Resolve conflicts if needed, then:
git branch -d new-branch  # Optional

"""